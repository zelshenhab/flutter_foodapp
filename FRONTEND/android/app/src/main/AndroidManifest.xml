<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ صلاحيات عامة -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ✅ صلاحيات الصور حسب نسخة أندرويد -->
    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 12L وأقدم -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ✅ للكاميرا (لو هتسمح بالتقاط صورة للأفاتار) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="flutter_foodapp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme تظهر أثناء إقلاع Flutter -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- لا تحذف - خاص بـ Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- 
      ✅ Android 11+ package visibility:
      وسّعنا الاستعلامات عشان نضمن إن Intents للاتصال/الميل/الروابط تشتغل بدون مشاكل.
      (ده آمن حتى لو بعضهم مش مستخدمين دايمًا)
    -->
    <queries>
        <!-- موجود مسبقًا لملحق معالجة النص -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

        <!-- فتح روابط https -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!-- اتصال هاتفي -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.CALL" />
            <data android:scheme="tel" />
        </intent>

        <!-- بريد إلكتروني -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
</manifest>
